version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npm install && CI=false npm run build
run:
  persistenceMount: /data
  scriptName: start
  containerPort: 3000 # ВАЖНО: Тут должен быть 3000!
serviceType: compute
