version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npm install && npm run build
  artifacts:
    build: build
run:
  persistenceMount: /data
  scriptName: ""
  scriptArguments: null
  nodeArguments: null
  command: npx serve -s build -p 80
  containerPort: 80
serviceType: compute
