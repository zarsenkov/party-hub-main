version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  install: npm install
  command: npm run build
  artifacts:
    build: build
run:
  persistenceMount: /data
  directory: build
  command: npx serve -s .
  containerPort: 3000
serviceType: compute
