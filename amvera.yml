version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  # Устанавливаем зависимости и собираем проект
  additionalCommands: npm ci && npm run build
run:
  persistenceMount: /data
  # Используем нашу готовую команду из package.json
  scriptName: start-server
  scriptArguments: null
  nodeArguments: null
  containerPort: 80
serviceType: compute
